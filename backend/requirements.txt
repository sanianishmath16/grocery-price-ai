fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.7.1
redis==5.0.4
python-dotenv==1.0.1
python-multipart==0.0.9
Pillow==10.3.0
openai==1.30.1
pytesseract==0.3.13
aiofiles==23.2.1

# ── Computer-vision (visual recognition for fresh produce) ────────────────
# CPU-only torch + torchvision.  The "+cpu" wheel is ~200 MB vs 900 MB for the
# GPU build.  We pin a specific index to guarantee the CPU wheel is chosen even
# if the standard PyPI build has a GPU default.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.7.1
redis==5.0.4
python-dotenv==1.0.1
python-multipart==0.0.9
Pillow==10.3.0
openai==1.30.1
pytesseract==0.3.13
aiofiles==23.2.1

# ── scikit-image for lightweight colour-based visual recognition ──────────
# ~20 MB install, ~20 MB RAM — safe for Render free tier (512 MB).
# Replaces the former torch/torchvision (200 MB) that caused OOM 502 errors.
numpy==1.26.4
